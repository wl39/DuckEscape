/* autogenerated by Processing revision 1290 on 2023-04-17 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.Arrays;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class DuckTreasure extends PApplet {



final int MAIN = 0;
final int GAME = 1;

public int state = MAIN;

int startButtonColor = color(255);
int startOverButtonColor = color(220);

int settingButtonColor = color(255);
int settingOverButtonColor = color(220);

int windowWidth = 1280;
int windowHeight = 720;
int pixelSize = 10;

boolean[][] pixelOn;
boolean testMode = true;

public void setup() {
    state = MAIN;
    background(255, 255, 255);
}

public void settings() {
    size(windowWidth, windowHeight);
    pixelOn = new boolean[windowWidth / pixelSize][windowHeight / pixelSize];
}

public void draw() {
    switch(state) {
        case MAIN:
            if (testMode) {
                testModeGrid();
                System.out.println(Arrays.toString(pixelOn));
            }
            break;
    }
}


public void testModeGrid() {
    for (int i = 0; i < windowHeight; i++) {
        for (int j = 0; j < windowWidth; j++) {
            rect(j * pixelSize, i * pixelSize, pixelSize, pixelSize);
        }
    }
    return;
}


  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "DuckTreasure" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
